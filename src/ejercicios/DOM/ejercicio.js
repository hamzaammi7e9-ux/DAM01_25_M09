/*function obtenStringEmpiezaConAs (string){
    var substring = string.substring(0,2)
    if(substring == `As`) {
        return string
    }
    else {
return `As${string}`
    } 

}

console.log(obtenStringEmpiezaConAs(`Cocacola`))
*/
/*
function eliminaCarracterPosicio(string, num){
    var resultado = ``
    if(num<=string.length){
        resultado = string.substring(num,num+1)
        return resultado
    }
}

console.log(eliminaCarracterPosicio(`Cocacola`, 2))
*/

function intercambiarPrimerYUltimoCaracter(string){
    var resultado = ``
    if(string.length>0){
        resultado = string.substring(num,num+1)
        return resultado
    }
}